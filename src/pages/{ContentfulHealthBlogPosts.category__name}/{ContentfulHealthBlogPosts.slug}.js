import React from 'react';
import Layout from '../../components/Layout';

const BlogPost = () => {
    return (
        <Layout>
            <h2>Blog Post</h2>
        </Layout>
    );
};

export default BlogPost;